<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rathon.manatee.database.mapper.IndustryMapper">
    <select id="findById" resultType="com.rathon.manatee.database.model.Industry">
        SELECT * FROM industries WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.rathon.manatee.database.model.Industry">
        SELECT * FROM industries
    </select>
    
    <insert id="insert" parameterType="com.rathon.manatee.database.model.Industry" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO industries (name) VALUES (#{name})
    </insert>

    <update id="update" parameterType="com.rathon.manatee.database.model.Industry">
        UPDATE industries SET name = #{name} WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM industries WHERE id = #{id}
    </delete>
</mapper>