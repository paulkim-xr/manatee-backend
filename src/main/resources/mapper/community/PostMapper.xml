<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rathon.manatee.community.mapper.PostMapper">

    <sql id="paging">
        <if test="size != null and offset != null">
            LIMIT #{size}
            OFFSET #{offset}
        </if>
    </sql>
    <sql id="sorting">
        ORDER BY
        <choose>
            <when test="sortColumn == 'id'">p.id</when>
            <when test="sortColumn == 'boardId'">p.board_id</when>
            <when test="sortColumn == 'authorId'">p.author_id</when>
            <when test="sortColumn == 'postedTime'">p.posted_time</when>
            <when test="sortColumn == 'editedTime'">p.edited_time</when>
            <when test="sortColumn == 'title'">p.title</when>
            <when test="sortColumn == 'content'">p.content</when>
            <when test="sortColumn == 'isAnnouncement'">p.is_announcement</when>
        </choose>
        <choose>
            <when test="sortDirection == 'desc'">DESC</when>
            <otherwise>ASC</otherwise>
        </choose>
    </sql>

    <select id="findById" resultType="Post">
        SELECT * FROM posts p
        WHERE id = #{id}
    </select>

    <select id="getPagedObjects" resultType="Post">
        SELECT * FROM posts p
        ORDER BY id
        <include refid="paging" />
    </select>

    <select id="findPostsByBoardId" resultType="Post">
        SELECT * FROM posts p
        WHERE board_id = #{id}
        <include refid="sorting" />
        <include refid="paging" />
    </select>

    <select id="countPostsByBoardId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM posts p
        WHERE board_id = #{id}
    </select>

    <select id="getCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM posts p
    </select>

    <insert id="insert" parameterType="Post" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts p
        (board_id, author_id, posted_time, edited_time, title, content, is_announcement)
        VALUES (#{boardId}, #{authorId}, #{postedTime}, #{editedTime}, #{title}, #{content}, #{isAnnouncement})
    </insert>

    <update id="update" parameterType="Post">
        UPDATE posts p SET
        board_id = #{boardId}, author_id = #{authorId}, posted_time = #{postedTime}, edited_time = #{editedTime}, title = #{title}, content = #{content}, is_announcement = #{isAnnouncement}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM posts p WHERE id = #{id}
    </delete>

    <select id="search" resultType="Post">
        SELECT * FROM posts p
        LEFT JOIN employees e ON p.author_id = e.id
        <choose>
            <when test="option == 0">
                WHERE
                (p.title ILIKE CONCAT('%', #{query}, '%') OR
                p.content ILIKE CONCAT('%', #{query}, '%'))
            </when>
            <when test="option == 1">
                WHERE
                p.title ILIKE CONCAT('%', #{query}, '%')
            </when>
            <when test="option == 2">
                WHERE
                CONCAT(e.last_name, e.first_name) ILIKE CONCAT('%', #{query}, '%')
            </when>
        </choose>
        <if test="id != null">
            AND p.board_id = #{id}
        </if>
        <include refid="sorting" />
        <include refid="paging" />
    </select>

    <select id="searchCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM posts p
        LEFT JOIN employees e ON p.author_id = e.id
        <choose>
            <when test="option == 0">
                WHERE
                (p.title ILIKE CONCAT('%', #{query}, '%') OR
                p.content ILIKE CONCAT('%', #{query}, '%'))
            </when>
            <when test="option == 1">
                WHERE
                p.title ILIKE CONCAT('%', #{query}, '%')
            </when>
            <when test="option == 2">
                WHERE
                CONCAT(e.last_name, e.first_name) ILIKE CONCAT('%', #{query}, '%')
            </when>
        </choose>
        <if test="id != null">
            AND p.board_id = #{id}
        </if>
    </select>
</mapper>