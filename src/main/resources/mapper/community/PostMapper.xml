<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rathon.manatee.community.mapper.PostMapper">

    <sql id="paging">
        <if test="size != null and offset != null">
            LIMIT #{size}
            OFFSET #{offset}
        </if>
    </sql>
    <sql id="sorting">
        <if test="sortColumn != null">
            ORDER BY
            <choose>
                <when test="sortColumn == 'id'">id</when>
                <when test="sortColumn == 'boardId'">board_id</when>
                <when test="sortColumn == 'authorId'">author_id</when>
                <when test="sortColumn == 'postedTime'">posted_time</when>
                <when test="sortColumn == 'editedTime'">edited_time</when>
                <when test="sortColumn == 'title'">title</when>
                <when test="sortColumn == 'content'">content</when>
                <when test="sortColumn == 'isAnnouncement'">is_announcement</when>
            </choose>
            <choose>
                <when test="sortDirection == 'desc'">DESC</when>
                <otherwise>ASC</otherwise>
            </choose>
        </if>
    </sql>

    <select id="findById" resultType="Post">
        SELECT * FROM posts
        WHERE id = #{id}
    </select>

    <select id="getPagedObjects" resultType="Post">
        SELECT * FROM posts
        ORDER BY id
        <include refid="paging" />
    </select>

    <select id="findPostsByBoardId" resultType="Post">
        SELECT * FROM posts
        WHERE board_id = #{id}
        <include refid="sorting" />
        <include refid="paging" />
    </select>

    <select id="countPostsByBoardId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM posts
        WHERE board_id = #{id}
    </select>

    <select id="getCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM posts
    </select>

    <insert id="insert" parameterType="Post" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts
        (board_id, author_id, posted_time, edited_time, title, content, is_announcement)
        VALUES (#{boardId}, #{authorId}, #{postedTime}, #{editedTime}, #{title}, #{content}, #{isAnnouncement})
    </insert>

    <update id="update" parameterType="Post">
        UPDATE posts SET
        board_id = #{boardId}, author_id = #{authorId}, posted_time = #{postedTime}, edited_time = #{editedTime}, title = #{title}, content = #{content}, is_announcement = #{isAnnouncement}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM posts WHERE id = #{id}
    </delete>

    <select id="search" resultType="Post">
        SELECT * FROM posts p
        LEFT JOIN employees e ON p.author_id = e.id
        WHERE
        <choose>
            <when test="option == 0">
                (p.title ILIKE CONCAT('%', #{query}, '%') OR
                p.content ILIKE CONCAT('%', #{query}, '%'))
            </when>
            <when test="option == 1">
                p.title ILIKE CONCAT('%', #{query}, '%')
            </when>
            <when test="option == 2">
                CONCAT(e.last_name, e.first_name) ILIKE CONCAT('%', #{query}, '%')
            </when>
        </choose>
        <if test="id != null">
            AND p.board_id = #{id}
        </if>
        <include refid="sorting" />
        <include refid="paging" />
    </select>

    <select id="searchCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM posts p
        LEFT JOIN employees e ON p.author_id = e.id
        <choose>
            <when test="option == 0">
                WHERE
                (p.title ILIKE CONCAT('%', #{query}, '%') OR
                p.content ILIKE CONCAT('%', #{query}, '%'))
            </when>
            <when test="option == 1">
                WHERE
                p.title ILIKE CONCAT('%', #{query}, '%')
            </when>
            <when test="option == 2">
                WHERE
                CONCAT(e.last_name, e.first_name) ILIKE CONCAT('%', #{query}, '%')
            </when>
        </choose>
        <if test="id != null">
            AND p.board_id = #{id}
        </if>
    </select>

    <update id="increaseViewCount">
        UPDATE posts SET
        view_count = (view_count + 1)
        WHERE id = #{id}
    </update>
</mapper>